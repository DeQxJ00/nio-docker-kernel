# Local source patches

No source-code patch was present in the inspected workspace, so this directory is intentionally empty. If a future kernel change cannot be expressed as Kconfig, add a numbered patch here and apply it after the pinned upstream checkout, before `apply-kernel-config.sh`; keep it small and document the upstream commit it targets.

